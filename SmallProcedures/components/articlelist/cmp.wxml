<view class="container">
    <article  class="article" wx:for="{{articleList}}" articleDetail="{{item}}" wx:key="item.artId" />
    <loading class="loading" hidden="{{!loading}}" />
    <text wx:if="{{noMoreData}}" class="no-more">没有更多数据了</text>
</view>
